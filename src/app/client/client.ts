export class Client {
   constructor(public idClient: string, public nomClient: string, public prenomClient: string, public mailClient: string,public telClient: number, public dateNaissance: Date, public author: string, public dateCINClient: Date, public cinClient: number, public typeClient: string) { 
   }
} 